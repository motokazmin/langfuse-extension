# 🔍 Langfuse AI Trace Analyzer

**Расширяем Langfuse AI-анализом трейсов**

Langfuse показывает *что* делают ваши AI-агенты. Мы объясняем *почему* и *как это исправить*.

---

## 🎯 Что это?

Система для автоматического анализа трейсов LLM-приложений. Один клик — и AI находит узкие места, избыточные затраты, логические циклы и ошибки в работе ваших агентов.

**Ключевые возможности:**
- 🔍 Автоматическая детекция 4 типов проблем (производительность, стоимость, циклы, ошибки)
- 💡 Конкретные рекомендации по оптимизации
- ⚡ Работает с любыми LLM провайдерами (OpenAI, Claude, Gemini, Ollama)
- 🎨 Бесшовная интеграция в UI Langfuse

---

## 🏗️ Архитектура

Проект состоит из трёх основных компонентов:

```
langfuse-ai-analyzer/
├── cursor-langfuse-ext/          # Интеграция Cursor IDE с Langfuse
├── chrome-ext/                   # Chrome расширение с кнопкой AI-анализа
└── ai-back/                      # Go backend для AI-анализа
```

### Как работает:

```
Cursor IDE → Langfuse (трейсы) → Chrome Extension (UI) → Go Backend (AI анализ) → Результат
```

1. **Cursor IDE** отправляет трейсы в Langfuse через hooks API
2. **Langfuse** хранит и показывает трейсы в своём UI
3. **Chrome Extension** добавляет кнопку "🤖 AI-Анализ" в интерфейс Langfuse
4. **Go Backend** получает trace ID, запрашивает полный трейс из Langfuse API, анализирует через LLM, возвращает структурированный результат
5. **Результат** отображается в модальном окне прямо в интерфейсе Langfuse

---

## 🚀 Быстрый старт

### 1. Backend (обязательно)

```bash
cd ai-back
cp .env.example .env
# Настройте .env (укажите Langfuse и AI provider ключи)
go run main.go
```

Backend запустится на `http://localhost:8080`

### 2. Chrome Extension (обязательно)

```bash
cd chrome-ext
npm install
npm run build
# Загрузите папку dist/ в Chrome через chrome://extensions/
```

### 3. Cursor Integration (опционально)

```bash
cd cursor-langfuse-ext
npm install
cp .env.example .env
# Настройте .env с Langfuse ключами
# Скопируйте папку .cursor в корень вашего проекта
```

Подробные инструкции по каждому компоненту — в соответствующих README.

---

## 📦 Компоненты проекта

### 🔗 [cursor-langfuse-ext](./cursor-langfuse-ext/)
Интеграция Cursor IDE с Langfuse через Hooks API.

**Основано на:** [naoufalelh/cursor-langfuse](https://github.com/naoufalelh/cursor-langfuse) (форк)

**Что изменено в форке:**
- Упрощена структура (убраны избыточные метаданные)
- Оптимизирована производительность (немедленный ответ Cursor)
- Улучшена стабильность имён трейсов

**Что делает:**
- Перехватывает основные действия AI в Cursor (промпты, мышление, правки файлов, shell команды)
- Отправляет в Langfuse как структурированные трейсы
- Каждый trace ID сохраняется в формате: имя трейса

**Важно:** Hooks устанавливаются в каждый проект отдельно (не глобально). Папка `.cursor/` должна быть в корне проекта.

**Документация:** [cursor-langfuse-ext/README.md](./cursor-langfuse-ext/README.md)

---

### 🎨 [chrome-ext](./chrome-ext/)
Chrome расширение для Langfuse UI.

**Что делает:**
- Детектирует страницы с трейсами в Langfuse (через URL polling каждые 100ms)
- Добавляет кнопку "🤖 AI-Анализ" (fixed position в правом верхнем углу)
- Отправляет trace ID на backend для анализа
- Показывает результаты в модальном окне с типизированными статусами

**Техническая реализация:**
- URL polling для работы с SPA навигацией Langfuse
- Сохранение trace ID в data-атрибуте (защита от изменения URL)
- CORS настройки для работы с Go backend

**Время анализа:** 2-5 секунд (зависит от AI провайдера)

**Документация:** [chrome-ext/README.md](./chrome-ext/README.md)

---

### ⚙️ [ai-back](./ai-back/)
Go backend для AI-анализа трейсов.

**Что делает:**
- Принимает HTTP запрос от Chrome Extension с trace ID
- Получает полный трейс из Langfuse API (с retry логикой)
- Отправляет на AI провайдер (OpenRouter или Ollama)
- Парсирует ответ и возвращает структурированный JSON

**Поддерживаемые AI провайдеры:**
- **OpenRouter:** Claude, GPT-4, Gemini (рекомендуется для production)
- **Ollama:** llama3.1:8b, qwen2.5:14b (для self-hosted)

**API Endpoints:**
- `GET /health` — проверка работоспособности
- `GET /analyze?traceId={id}` — анализ трейса

**Документация:** [ai-back/README.md](./ai-back/README.md)

---

## 🎓 Типы детектируемых проблем

### 1. 🐌 PERFORMANCE_BOTTLENECK
Медленные операции (latency > 10s или одна операция занимает >70% времени).

**Пример из документации:**
```
Shell команда 'npm install' выполнялась 12.3с (82% времени)
→ Рекомендация: Используйте npm ci в CI/CD — в 2-3 раза быстрее
```

### 2. 💸 HIGH_COST
Избыточные затраты (totalCost > $0.20 или >5000 токенов на простой запрос).

**Пример из документации:**
```
8,450 токенов для ответа на простой вопрос
→ Рекомендация: Агент загрузил 5 ненужных файлов в контекст
```

### 3. 🔄 LOGICAL_LOOP
Зацикливание (операция повторяется >3 раз с похожими результатами).

**Пример из документации:**
```
Агент 5 раз редактировал auth.js, тесты падали с той же ошибкой
→ Рекомендация: Добавьте в промпт анализ всех импортов
```

### 4. ❌ ERROR
Ошибки выполнения (exit code != 0, exceptions).

**Пример из документации:**
```
Компиляция завершилась с ошибкой: syntax error в main.go:42
→ Рекомендация: AI предложил код с ошибкой, добавьте проверку компиляции
```

### 5. ✅ NONE
Всё в порядке, проблем не обнаружено.

---

## 🛠️ Технологии

**Backend:**
- Go 1.21+
- Gin (HTTP framework)
- Langfuse SDK (для получения трейсов)

**Chrome Extension:**
- TypeScript
- Vite (сборка)
- Chrome Extensions Manifest V3

**Cursor Integration:**
- Node.js
- Langfuse SDK

**AI Providers:**
- OpenRouter (Claude 3.5 Sonnet, GPT-4, Gemini 2.0 Flash)
- Ollama (llama3.1:8b, qwen2.5:14b, llama3.1:70b)

---

## 📊 Формат анализа

Backend возвращает структурированный JSON:

```json
{
  "analysisSummary": {
    "overallStatus": "WARNING",
    "keyFinding": "Shell команда занимает 80% времени"
  },
  "detailedAnalysis": {
    "anomalyType": "PERFORMANCE_BOTTLENECK",
    "description": "Shell команда 'npm install' выполнялась 12.3с (82% времени)",
    "rootCause": "npm install загружает зависимости с нуля",
    "recommendation": "Используйте npm ci в CI/CD — в 2-3 раза быстрее"
  },
  "metadata": {
    "traceId": "f7b61b34-3a44-4146-9bd8-9f60fd788831",
    "analyzedAt": "2026-01-21T15:30:45Z",
    "processingTime": 2.3,
    "model": "anthropic/claude-3.5-sonnet",
    "provider": "openrouter"
  }
}
```

**Статусы:**
- `HEALTHY` — всё в порядке (зелёный)
- `WARNING` — найдены неоптимальности (жёлтый)
- `ERROR` — критические ошибки (красный)

---

## 🔐 Безопасность и приватность

### Что отправляется на backend

**Chrome Extension → Backend:**
- Только trace ID (например: `f7b61b34-3a44-4146-9bd8-9f60fd788831`)

**Не отправляется:**
- Содержимое трейса
- Langfuse credentials
- Cookies или личные данные

### Что отправляется на AI провайдер

**Backend → AI Provider (OpenRouter/Ollama):**
- Полный JSON трейса из Langfuse (включает промпты, ответы, метаданные, токены)

**Рекомендации по безопасности:**
- ✅ Для personal проектов: Langfuse Cloud + OpenRouter OK
- ✅ Для корпоративных данных: self-hosted Langfuse + Ollama
- ✅ Для sensitive данных: используйте только Ollama (данные не покидают машину)

### Настройка CORS

Backend требует настройки CORS для работы с Chrome Extension:

```env
ALLOWED_ORIGINS=https://cloud.langfuse.com,chrome-extension://YOUR_EXTENSION_ID
```

Для self-hosted Langfuse добавьте свой домен в `ALLOWED_ORIGINS`.

---

## 📚 Документация компонентов

- **Cursor Integration:** [cursor-langfuse-ext/README.md](./cursor-langfuse-ext/README.md)
- **Chrome Extension:** [chrome-ext/README.md](./chrome-ext/README.md)
- **Backend:** [ai-back/README.md](./ai-back/README.md)

### Внешние ресурсы

- **Langfuse API:** https://langfuse.com/docs/api
- **Cursor Hooks:** https://docs.cursor.com/context/hooks
- **OpenRouter:** https://openrouter.ai/docs
- **Ollama:** https://ollama.com/

---

## 🙏 Благодарности

- [Langfuse](https://langfuse.com) — за отличную платформу observability
- [naoufalelh/cursor-langfuse](https://github.com/naoufalelh/cursor-langfuse) — за базовую интеграцию Cursor

---

## 📄 Лицензия

MIT License - см. [LICENSE](./LICENSE)