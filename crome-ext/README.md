# Chrome Extension для Langfuse

Добавляет кнопку AI-анализа в интерфейс Langfuse для автоматического обнаружения проблем в трейсах.

---

## 🎯 Что это даёт?

Расширение интегрируется в веб-интерфейс Langfuse и добавляет один клик для анализа:

1. **Автоматически детектирует трейсы** — определяет когда вы смотрите на трейс
2. **Добавляет кнопку анализа** — появляется в правом верхнем углу
3. **Запрашивает AI-анализ** — отправляет trace ID на backend
4. **Показывает результаты** — открывает модальное окно с рекомендациями

**Результат:** вместо ручного анализа трейса, AI находит проблемы и предлагает решения за 2-5 секунд.

---

## 📋 Требования

- **Браузер:** Chrome, Edge или любой Chromium-браузер
- **Backend:** Запущенный Go backend (см. [ai-back](../ai-back/))
- **Langfuse:** Доступ к cloud.langfuse.com или self-hosted инстансу

---

## 🚀 Установка

### Шаг 1: Соберите расширение

```bash
cd chrome-ext
npm install
npm run build
```

Это создаст папку `dist/` с готовым расширением.

### Шаг 2: Загрузите в браузер

1. Откройте `chrome://extensions/`
2. Включите **"Режим разработчика"** (Developer mode) — переключатель в правом верхнем углу
3. Нажмите **"Загрузить распакованное расширение"** (Load unpacked)
4. Выберите папку `chrome-ext/dist/`

Расширение появится в списке установленных.

---

## ✅ Проверка работы

### Шаг 1: Откройте Langfuse

Перейдите на https://cloud.langfuse.com (или ваш self-hosted инстанс).

### Шаг 2: Откройте любой трейс

Кликните на трейс в списке. URL будет выглядеть так:
```
https://cloud.langfuse.com/project/xxx/traces?peek=trace-id-here
```

### Шаг 3: Найдите кнопку

В правом верхнем углу появится кнопка **"🤖 AI-Анализ"**.

> Если кнопка не появилась — проверьте что backend запущен и расширение активно в `chrome://extensions/`

### Шаг 4: Запустите анализ

Кликните на кнопку. Появится модальное окно:

**Во время анализа:**
```
🤖 Analyzing trace...
Please wait, AI is thinking...
```

**После анализа (пример):**
```
✅ HEALTHY
━━━━━━━━━━━━━━━━
Трейс выполнен успешно без проблем
```

или

```
⚠️ WARNING
━━━━━━━━━━━━━━━━
PERFORMANCE_BOTTLENECK

Shell команда 'npm test' заняла 80% времени (12.3s из 15s)

🔍 Причина:
npm test запускает все тесты, включая медленные интеграционные

💡 Рекомендация:
Запускайте только релевантные тесты: npm test -- auth.test.js
Или используйте watch mode для быстрой итерации
━━━━━━━━━━━━━━━━
Analyzed 2.3s ago
```

---

## 🔧 Как это работает

### Общая схема

```
Langfuse UI (Next.js SPA)
    ↓
Content Script отслеживает URL
    ↓ каждые 100ms
Детектирует trace ID в URL
    ↓
Создаёт кнопку "AI-Анализ"
    ↓ onClick
Отправляет GET запрос на backend
    ↓
Backend возвращает JSON с анализом
    ↓
Показывает модальное окно
```

### Техническая реализация

#### 1. URL Polling для SPA

**Проблема:** Langfuse — это Next.js SPA с client-side routing. При переходе между трейсами страница не перезагружается.

**Решение:** Проверяем URL каждые 100ms и детектируем изменения:

```javascript
let lastUrl = window.location.href;

setInterval(() => {
  const currentUrl = window.location.href;
  
  if (currentUrl !== lastUrl) {
    lastUrl = currentUrl;
    
    // Проверяем наличие trace ID
    const traceId = extractTraceId(currentUrl);
    if (traceId) {
      createAnalyzeButton(traceId);
    }
  }
}, 100);
```

**Overhead:** ~0.1ms на проверку, незаметно для пользователя.

#### 2. Сохранение Trace ID

**Проблема:** Между созданием кнопки и кликом пользователь может закрыть модальное окно трейса в Langfuse, и URL изменится.

**Решение:** Сохраняем trace ID в data-атрибуте кнопки при создании:

```typescript
// При создании кнопки
button.dataset.traceId = currentTraceId;

// При клике — берём сохранённый ID
const traceId = button.dataset.traceId;
```

Теперь trace ID "заморожен" и не теряется при изменении URL.

#### 3. Fixed Position кнопки

Кнопка не зависит от DOM-структуры Langfuse:

```css
.ai-analyzer-button {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
}
```

Это гарантирует что кнопка всегда видна и не конфликтует с интерфейсом.

---

## 🔄 Процесс анализа

### Шаг за шагом

**1. Пользователь кликает "🤖 AI-Анализ"**

```javascript
handleAnalyzeClick() {
  const traceId = button.dataset.traceId;
  showLoadingModal("🤖 Analyzing trace...");
  sendAnalysisRequest(traceId);
}
```

**2. Расширение отправляет запрос**

```http
GET http://localhost:8080/analyze?traceId=f7b61b34-3a44-4146-9bd8-9f60fd788831
```

**3. Backend обрабатывает** (подробности в [ai-back/README.md](../ai-back/)):
- Получает полный трейс из Langfuse API
- Отправляет на AI провайдер
- Парсит результат
- Возвращает структурированный JSON

**4. Backend возвращает результат**

```json
{
  "analysisSummary": {
    "overallStatus": "WARNING",
    "keyFinding": "Shell команда занимает 80% времени"
  },
  "detailedAnalysis": {
    "anomalyType": "PERFORMANCE_BOTTLENECK",
    "description": "Shell команда 'npm install' выполнялась 12.3с",
    "rootCause": "npm install загружает зависимости с нуля",
    "recommendation": "Используйте npm ci в CI/CD — в 2-3 раза быстрее"
  },
  "metadata": {
    "traceId": "f7b61b34-...",
    "analyzedAt": "2026-01-21T15:30:45Z",
    "processingTime": 2.3
  }
}
```

**5. Расширение показывает результат**

Парсит JSON и рендерит в модальном окне с цветовой индикацией статуса.

---

## 📊 Типы результатов

### Статусы

| Статус | Цвет | Когда появляется |
|--------|------|------------------|
| ✅ HEALTHY | Зелёный | Проблем не найдено |
| ⚠️ WARNING | Жёлтый | Неоптимальности, но работает |
| ❌ ERROR | Красный | Критические ошибки |

### Типы аномалий

1. **PERFORMANCE_BOTTLENECK** — медленные операции (>70% времени)
2. **HIGH_COST** — избыточные затраты на токены
3. **LOGICAL_LOOP** — зацикливание агента (>3 повторов)
4. **ERROR** — ошибки выполнения (exit code != 0)
5. **NONE** — всё в порядке

---

## ⏱️ Время анализа

Типичное время обработки по этапам:

| Этап | Время |
|------|-------|
| Запрос → Backend | ~50ms |
| Backend получает трейс из Langfuse | ~200ms |
| AI анализ (OpenRouter) | 2-4s |
| AI анализ (Ollama локально) | 5-10s |
| Возврат результата → UI | ~50ms |
| **Итого** | **2-5 секунд** |

Во время анализа показывается индикатор загрузки с анимацией.

---

## ⚙️ Конфигурация

### Backend URL

По умолчанию: `http://localhost:8080`

Если backend на другом хосте/порту, измените в `src/config.ts`:

```typescript
export const BACKEND_URL = "http://your-backend:8080";
```

### CORS настройка

**⚠️ Важно:** Backend должен разрешать CORS запросы от расширения.

В Go backend добавьте:

```go
w.Header().Set("Access-Control-Allow-Origin", "chrome-extension://YOUR_EXTENSION_ID")
w.Header().Set("Access-Control-Allow-Methods", "GET, OPTIONS")
```

Или используйте middleware `github.com/rs/cors` для автоматической обработки.

**Как узнать ID расширения:**
1. Откройте `chrome://extensions/`
2. Найдите "Chrome Extension для Langfuse"
3. Скопируйте **ID:** из карточки расширения

### Self-hosted Langfuse

Для работы с self-hosted Langfuse добавьте ваш домен в `manifest.json`:

```json
{
  "host_permissions": [
    "https://cloud.langfuse.com/*",
    "https://your-langfuse.com/*"
  ]
}
```

После изменений пересоберите: `npm run build`

---

## 🔒 Безопасность и приватность

### Минимальные разрешения

Расширение запрашивает только необходимое:

```json
{
  "permissions": ["storage"],
  "host_permissions": [
    "https://cloud.langfuse.com/*",
    "http://localhost:8080/*"
  ]
}
```

**Что означают разрешения:**
- `storage` — сохранение настроек (URL backend)
- `cloud.langfuse.com` — доступ к интерфейсу Langfuse
- `localhost:8080` — отправка запросов на локальный backend

### Что отправляется

**На backend:**
- ✅ Только trace ID (строка ~36 символов)

**НЕ отправляется:**
- ❌ Содержимое трейса
- ❌ Langfuse credentials
- ❌ Cookies
- ❌ История браузера
- ❌ Личные данные

Весь контент трейса backend получает напрямую из Langfuse API, минуя расширение.

### Open Source

- Весь код доступен для аудита
- Нет аналитики или трекинга
- Нет отправки данных третьим лицам
- Работает только на страницах Langfuse

---

## 🐛 Troubleshooting

### Кнопка не появляется

**Решения:**
1. Проверьте что расширение активно: `chrome://extensions/`
2. Убедитесь что находитесь на странице трейса (URL содержит `peek=trace-id`)
3. Обновите страницу (Ctrl+R)
4. Перезагрузите расширение: `chrome://extensions/` → кнопка обновить

### "Failed to analyze"

**Причина:** Backend недоступен или вернул ошибку.

**Решения:**
1. Проверьте что backend запущен: `curl http://localhost:8080/health`
2. Проверьте консоль браузера (F12) на ошибки CORS
3. Убедитесь что CORS настроен в backend (см. секцию Конфигурация)

### Медленный анализ (>30s)

**Причины:**
1. Ollama на слабом железе
2. Большой трейс (>100 observations)
3. Сетевые проблемы

**Решения:**
- Переключитесь на OpenRouter (быстрее Ollama)
- Используйте модель `gemini-2.0-flash` (самая быстрая)
- Проверьте сеть: `ping openrouter.ai`

---

## 📚 Дополнительные ресурсы

- **Chrome Extensions API:** https://developer.chrome.com/docs/extensions/
- **Manifest V3:** https://developer.chrome.com/docs/extensions/mv3/
- **Content Scripts:** https://developer.chrome.com/docs/extensions/mv3/content_scripts/

---

**Следующий шаг:** Настройте [Backend](../ai-back/) для обработки запросов на анализ!