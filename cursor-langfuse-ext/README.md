# Cursor → Langfuse Integration

Интеграция Cursor IDE с Langfuse для автоматической отправки трейсов работы AI-ассистента.

---

## 📌 О проекте

**Форк:** [naoufalelh/cursor-langfuse](https://github.com/naoufalelh/cursor-langfuse)

Этот форк оптимизирован для производительности и простоты использования.

### Ключевые отличия от оригинала

| Параметр | Этот форк | Оригинал |
|----------|-----------|----------|
| Структура | Минималистичная | Расширенная (12 типов hooks) |
| Метаданные | Только необходимые | Детальная статистика |
| Производительность | Немедленный ответ Cursor | Стандартная |
| Стабильность имён | Улучшенная | Базовая |

### Когда использовать что?

**Этот форк подходит если:**
- Нужна базовая функциональность без избыточности
- Важна максимальная производительность
- Предпочитаете минимализм

**Оригинал подходит если:**
- Нужны все типы событий (включая Tab)
- Требуется детальная статистика и метаданные
- Нужна расширенная конфигурация

```bash
# Использовать оригинальный проект:
git clone https://github.com/naoufalelh/cursor-langfuse.git
cd cursor-langfuse
npm install
```

---

## 🎯 Что перехватывается?

Hooks отслеживают основные действия AI в Cursor:

- 💬 **Промпты пользователя** — каждый ваш запрос к AI
- 🧠 **"Мышление" AI** — внутренний процесс обработки
- 📝 **Правки файлов** — все изменения кода
- 🖥️ **Shell команды** — запуск скриптов и тестов
- ✅ **Завершение генерации** — финальный результат

Каждое действие сохраняется как отдельный span/generation в трейсе.

---

## 📋 Требования

- Cursor IDE (любая версия)
- Node.js 18 или выше
- Аккаунт Langfuse (cloud или self-hosted)

---

## 🚀 Установка

### Шаг 1: Установите зависимости

```bash
cd cursor-langfuse-ext
npm install
```

### Шаг 2: Настройте переменные окружения

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_BASEURL=https://cloud.langfuse.com
```

**Где взять ключи:**
1. Откройте https://cloud.langfuse.com
2. Перейдите в Settings → API Keys
3. Нажмите Create new
4. Скопируйте Public Key и Secret Key

### Шаг 3: Установите hooks в проект

**⚠️ Важно:** Hooks работают на уровне проекта, не глобально.

Перейдите в корень вашего проекта:
```bash
cd ~/my-project
```

Скопируйте конфигурацию:
```bash
# Скопируйте папку .cursor
cp -r /path/to/cursor-langfuse-ext/.cursor ./

# Скопируйте .env
cp /path/to/cursor-langfuse-ext/.env ./
```

**Правильная структура проекта:**
```
my-project/
├── .cursor/
│   └── hooks/
│       ├── hook-handler.js
│       └── lib/
│           └── langfuse-client.js
├── .env                    # ← Langfuse ключи
├── src/
└── ...
```

**Для нескольких проектов:**
Повторите копирование в каждый проект. Можно использовать один `.env` или разные для отправки в разные Langfuse проекты.

### Шаг 4: Перезапустите Cursor

Закройте и откройте Cursor IDE для активации hooks.

---

## ✅ Проверка работы

Отправьте любой промпт в Cursor:
```
Напиши Hello World на Python
```

Откройте Langfuse:
1. Перейдите на https://cloud.langfuse.com
2. Откройте раздел Traces
3. Найдите новый трейс с вашим промптом

**Ожидаемая структура трейса:**
```
Trace: "Напиши Hello World на Python"
├─ Generation: User Prompt
│  Input: "Напиши Hello World на Python"
│  Model: claude-sonnet-3.5
│
├─ Span: Agent Thinking
│  Duration: ~0.8s
│
├─ Generation: Code Generation  
│  Output: print("Hello World")
│
└─ Span: File Edit
   File: hello.py
   Lines: +3
```

---

## 🎨 Структура трейса

Каждое взаимодействие с Cursor создаёт трейс со следующими элементами:

| Элемент | Тип | Содержит |
|---------|-----|----------|
| User Prompt | Generation | Текст вашего запроса |
| Agent Thinking | Span | Время обработки AI |
| Code Generation | Generation | Сгенерированный код |
| File Edit | Span | Изменённые файлы и строки |
| Shell Execution | Span | Выполненные команды |

**Метаданные в каждом трейсе:**
- Модель AI (claude-sonnet-3.5, gpt-4, etc.)
- Длительность операций
- Количество токенов
- Статус выполнения

---

## 🔒 Безопасность

### Что отправляется в Langfuse

**Отправляется:**
- ✅ Текст промптов
- ✅ Сгенерированный код
- ✅ Имена файлов
- ✅ Shell команды
- ✅ Метаданные (модель, токены, время)

**Не отправляется:**
- ❌ Содержимое файлов, которые не редактировались
- ❌ Ваши пароли или API ключи (если не были в промпте)
- ❌ История браузера или системные данные

### Рекомендации

**Personal проекты:**
- Langfuse Cloud + этот форк = OK
- Все данные в надёжном observability сервисе

**Корпоративные проекты:**
- Используйте self-hosted Langfuse
- Разверните на своих серверах
- Полный контроль над данными

**Настройка self-hosted:**
```env
LANGFUSE_BASEURL=https://your-langfuse-instance.com
```

---

## 🔧 Конфигурация

### Путь к .env

`langfuse-client.js` ищет `.env` в корне проекта (3 уровня вверх от `lib/`):

```
my-project/
├── .cursor/hooks/lib/langfuse-client.js  ← отсюда
└── .env                                   ← ищет здесь (3 уровня вверх)
```

Если `.env` в другом месте, измените путь в `langfuse-client.js`.

### Переменные окружения

| Переменная | Обязательно | Описание |
|------------|-------------|----------|
| `LANGFUSE_PUBLIC_KEY` | Да | Public API key из Langfuse |
| `LANGFUSE_SECRET_KEY` | Да | Secret API key из Langfuse |
| `LANGFUSE_BASEURL` | Да | URL инстанса Langfuse |

---

## 🐛 Troubleshooting

### Трейсы не появляются

**Проблема:** Отправили промпт, но трейса нет в Langfuse.

**Решения:**
1. Проверьте `.env` в корне проекта
2. Убедитесь что ключи правильные (скопируйте заново)
3. Проверьте `LANGFUSE_BASEURL` (без `/` в конце)
4. Перезапустите Cursor

**Проверка конфигурации:**
```bash
# В корне проекта
cat .env | grep LANGFUSE
```

### Hooks не активны

**Проблема:** Cursor не вызывает hooks.

**Решения:**
1. Убедитесь что папка `.cursor/` в корне проекта
2. Проверьте права доступа: `chmod +x .cursor/hooks/hook-handler.js`
3. Полностью перезапустите Cursor (не только окно)

### Ошибки в консоли

**Проблема:** Cursor показывает ошибки связанные с hooks.

**Решение:**
- Проверьте `npm install` выполнен в `cursor-langfuse-ext/`
- Убедитесь что Node.js 18+: `node --version`
- Проверьте логи: `.cursor/hooks/logs/` (если есть)

---

## 📚 Дополнительные ресурсы

- **Langfuse документация:** https://langfuse.com/docs
- **Cursor Hooks API:** https://docs.cursor.com/context/hooks
- **Оригинальный проект:** https://github.com/naoufalelh/cursor-langfuse

---

**Следующий шаг:** Установите [Chrome Extension](../chrome-ext/) для AI-анализа трейсов!